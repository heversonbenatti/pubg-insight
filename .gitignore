.env
node_modules/
/.claude
api-documentation.md
CLAUDE.md

# Cache local (gera no primeiro request) — pesado e regenerável
/public/jsons/

# Inputs do pipeline de build (FModel extract + tiles gen) — regerar quando virar temporada
/Blueprints/
/scripts/output/
/public/images/maps/

# pubg-api-assets: Maps usa Git LFS (ponteiros sem binários locais — push falha).
# Resto da pasta vai pro repo normal.
/pubg-api-assets/Assets/Maps/
